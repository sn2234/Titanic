
import Data.Matrix
import Control.Monad
import Debug.Trace

import DataModel
import LogisticRegression

-- Process data set and produce X and Y for logistic regression
processDataModel :: [DataRec] -> (Matrix Double, Matrix Double)
processDataModel = undefined
